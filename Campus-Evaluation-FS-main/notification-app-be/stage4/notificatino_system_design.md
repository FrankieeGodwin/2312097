The DB is getting overwelmed by the users loading notificatons on each page.
This creates bad user experience and is an issue.

To clear this in frontend lazy loading feature can be added. 
only fixed amount of notifications are loaded and others are loaded only when needed.

in server side vertical scaling can be used in the same institution we can increase the server power and use techniques like sharding to provide everything to everyone overtime without performance delay.

This Solution adderesses the probelm mentioned above.